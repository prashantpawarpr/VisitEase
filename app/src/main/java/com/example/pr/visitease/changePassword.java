package com.example.pr.visitease;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.EditText;

import com.parse.ParseObject;
import com.parse.ParseQuery;
import com.parse.ParseUser;

public class changePassword extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_change_password);

//        EditText oldpassword = (EditText) findViewById(R.id.oldpasswordEditText);
//        EditText password = (EditText) findViewById(R.id.passwordEditText);
//        EditText newpassword = (EditText)findViewById(R.id.oldpasswordEditText);
//
//
//        // get current username
//        String user = ParseUser.getCurrentUser().getUsername();
//        String pass = ParseUser.getCurrentUser().getPassword

//
//        ParseQuery<ParseObject> query = ParseQuery.getQuery("User");
//        query.whereEqualTo("username",user);
//        query.findInBackground(new)








    }
}